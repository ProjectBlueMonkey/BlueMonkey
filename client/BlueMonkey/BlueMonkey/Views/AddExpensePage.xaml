<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="BlueMonkey.Views.AddExpensePage" Title="Expense">

  <ContentPage.ToolbarItems>
    <ToolbarItem Text="Save" Command="{Binding SaveCommand}"/>
  </ContentPage.ToolbarItems>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <StackLayout Grid.Row="0" HorizontalOptions="Start" VerticalOptions="Start" Padding="10">
      <Label Text="{Binding Text}"/>
      <Button Text="Receipt" Command="{Binding NavigateCommand}" />
      <Label Text="Category" HorizontalOptions="Start"/>
      <Label Text="Amount" HorizontalOptions="Start"/>
      <Entry HorizontalOptions="Fill" HorizontalTextAlignment="End"/>
      <Label Text="Date"/>
      <DatePicker/>
      <Label Text="Location"/>
      <Entry/>
      <Label Text="Note"/>
    </StackLayout>
    <Grid Grid.Row="1" Padding="10">
      <Editor/>
    </Grid>
  </Grid>

</ContentPage>